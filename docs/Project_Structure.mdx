# Project Structure Documentation
## Glade - Privacy-Focused Social Media Platform

**Document Version**: 1.0  
**Date**: October 4, 2025  
**Purpose**: Define clear project organization and file structure conventions

---

## Current Architecture

**Structure:** Monorepo
**Design:** Loosely Layered


## Current Structure

```
Glade/
├── backend/                          # Server-side applications
│   ├── glade/                        # Main Django project
│   │   ├── settings/                 # Environment-specific settings
│   │   │   ├── __init__.py           # settings module init
│   │   │   ├── base.py               # base configuration
│   │   │   ├── development.py        # development environment
│   │   │   └── production.py         # production environment
│   │   ├── urls.py                   # main url routing
│   │   ├── wsgi.py                   # wsgi application
│   │   ├── asgi.py                   # asgi application (async)
│   │   └── celery_app.py             # celery configuration
│   │
│   ├── accounts/                     # User Authentication & Management
│   │   ├── models.py                 # user, follow, email verification models
│   │   ├── views.py                  # authentication & user api endpoints
│   │   ├── serializers.py            # request/response schemas
│   │   ├── urls.py                   # account-related routes
│   │   ├── admin.py                  # admin interface config
│   │   └── apps.py                   # django app configuration
│   │
│   ├── posts/                        # Content Management
│   │   ├── models.py                 # post, like models
│   │   ├── views.py                  # post api endpoints
│   │   ├── serializers.py            # post schemas
│   │   ├── urls.py                   # post routes
│   │   ├── admin.py                  # admin interface
│   │   └── apps.py                   # django app configuration
│   │
│   ├── federation/                   # ActivityPub Protocol
│   │   ├── models.py                 # remote instance, remote user, activity
│   │   ├── views.py                  # activitypub endpoints
│   │   ├── services.py               # federation business logic
│   │   ├── tasks.py                  # celery tasks for federation
│   │   ├── urls.py                   # federation routes
│   │
│   ├── notifications/                # Notification System
│   │   ├── models.py                 # notification, notificationpreference
│   │   ├── views.py                  # notification api endpoints
│   │   ├── serializers.py            # notification schemas
│   │   ├── services.py               # notification business logic
│   │   ├── tasks.py                  # celery tasks for notifications
│   │   └── urls.py                   # notification routes
│   │
│   ├── privacy/                      # Privacy Controls
│   │   ├── middleware.py             # privacy middleware
│   │   └── services.py               # privacy business logic
│   │
│   ├── services/                     # 🟡 SHARED: Business logic services
│   │   ├── validation_service.py     # input validation and sanitization
│   │   ├── email_service.py          # email functionality
│   │   └── security_service.py       # security utilities
│   │
│   ├── templates/                    # Django templates
│   │   └── emails/                   # Email templates
│   │       ├── notification.html     # notification email
│   │       ├── password_reset.html   # password reset email
│   │       └── verify_email.html     # email verification
│   │
│   ├── manage.py                     # django management script
│   └── .env                          # backend environment variables
│
├── frontend/                         # React frontend application
│   ├── src/
│   │   ├── components/               # react components
│   │   ├── hooks/                    # custom react hooks
│   │   ├── pages/                    # page components
│   │   ├── services/                 # api client services
│   │   ├── App.jsx                   # main application component
│   │   ├── main.jsx                  # application entry point
│   │   └── index.css                 # global styles
│   ├── package.json                  # node.js dependencies
│   ├── vite.config.js                # vite build configuration
│   ├── tailwind.config.js            # tailwind css configuration
│   └── index.html                    # html entry point
│
├── infrastructure/                   # Deployment and infrastructure
│   └── docker/                       # Container configurations
│       ├── docker-compose.yml        # base multi-service orchestration (dev)
│       ├── docker-compose.override.yml # dev-specific overrides
│       ├── Dockerfile.backend        # backend container
│       ├── Dockerfile.frontend       # frontend container
│       └── nginx.conf                # reverse proxy configuration
│
├── requirements/                     # Python dependencies
│   ├── base.txt                      # production dependencies
│   ├── development.txt               # dev tools (debug toolbar, testing, etc.)
│   └── production.txt                # production-specific packages
│
├── scripts/                          # Utility scripts
│   ├── demo-data.py                  # create demo data for testing
│   ├── dev-setup.sh                  # one-time development environment setup
│   └── quick-setup.sh                # quick development setup (legacy)
│
├── docs/                             # Documentation (internal/external)
│   ├── Development_Setup_Guide.md
│   ├── Technical_Requirements_Document.mdx
│   ├── database-models-uml.md        
│   └── Project_Structure.mdx         # this file
│
├── .gitignore
├── LICENSE                           
├── README.md                         
```

---

## File Naming Conventions

### **Services:**
- `{domain}_service.py` (e.g., `account_service.py`)
- Class: `{Domain}Service` (e.g., `AccountService`)

### **Repositories:**
- `{domain}_repository.py` (e.g., `user_repository.py`)
- Class: `{Domain}Repository` (e.g., `UserRepository`)

### **Models:**
- `{domain}.py` (e.g., `user.py`, `post.py`)
- Class: `{Entity}` (e.g., `User`, `Post`)

### **API:**
- `views.py`, `serializers.py`, `urls.py` per domain

---

## Import Conventions

PEP 8 generally recommends absolute imports, Django projects follow a specific convention to ensure apps are reusable and self-contained.

[https://docs.djangoproject.com/en/5.2/intro/reusable-apps/](https://docs.djangoproject.com/en/5.2/intro/reusable-apps/)

- **Intra-App Imports (within the same app):** Use **explicit relative imports**. This prevents tying an app to a specific project name, making it modular and reusable.

  ```python
  # Correct: In accounts/views.py
  from .models import User
  from .serializers import UserSerializer
  ```

- **Inter-App Imports (between different apps):** Use **absolute imports**. This makes dependencies between apps clear and explicit.

  ```python
  # Correct: In posts/views.py, importing a model from the accounts app
  from accounts.models import User
  ```
---

**Document Control**
- **Next Review**: Weekly during development
- **Approval**: Development team lead
- **Distribution**: Public